{
  "guid": "trdt",
  "name": "Transient Detector",
  "short_description": "Detects transients",
  "description": "This algorithm generates triggers from transients in audio signals. It is essentially the same code that detects transients in the Kirbinator and in the Sample Player (Sliced) algorithms, broken out into a separate algorithm. While designed for use on audio, we have found it also gives good results when applied to the signals generated by piezo-electric pickups, for example, the simple electronic pickups that can be clipped to drums.",
  "categories": [
    "Detection",
    "Trigger",
    "Utility"
  ],
  "parameters": [
    {
      "name": "Enable",
      "min": 0,
      "max": 1,
      "default": 0,
      "description": "Enables the channel."
    },
    {
      "name": "Trigger output mode",
      "min": 0,
      "max": 1,
      "default": 1,
      "description": "The standard Add/Replace mode selector as described above, for the trigger."
    },
    {
      "name": "Level output mode",
      "min": 0,
      "max": 1,
      "default": 1,
      "description": "The standard Add/Replace mode selector as described above, for the level."
    },
    {
      "name": "Gain",
      "min": -40,
      "max": 24,
      "default": 0,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "Sensitivity",
      "min": 0,
      "max": 200,
      "default": 100,
      "type": "folder",
      "is_bus": false
    }
  ],
  "input_ports": [
    {
      "name": "Input",
      "description": "The input bus to process."
    },
    {
      "name": "Input",
      "description": "The input bus to process."
    }
  ],
  "output_ports": [
    {
      "name": "Trigger output",
      "description": "The output bus for the trigger signal."
    },
    {
      "name": "Level output",
      "description": "The output bus for the level (velocity) signal."
    },
    {
      "name": "Trigger output",
      "description": "The output bus for the trigger signal."
    },
    {
      "name": "Level output",
      "description": "The output bus for the level (velocity) signal."
    }
  ]
}