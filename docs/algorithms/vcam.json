{
  "guid": "vcam",
  "name": "VCA/Multiplier",
  "short_description": "Four quadrant multiplier",
  "description": "This algorithm is a voltage multiplier, which can be used as a VCA. The CV on the common channel is used to multiply the voltages on the other channels. The algorithm supports zero-cross detection, in the manner of our Persephone 134 analogue VCA module.",
  "categories": [
    "VCA",
    "Multiplier",
    "Mixer"
  ],
  "parameters": [
    {
      "name": "Clamp to 0V",
      "min": 0,
      "max": 1,
      "default": 1,
      "scope": "common",
      "description": "Whether to clamp the common input to 0V (typical VCA usage) or not (four quadrant multiplier usage)."
    },
    {
      "name": "Divider",
      "min": 1,
      "max": 12,
      "default": 8,
      "scope": "common",
      "description": "Sets the scaling of the input CV that corresponds to 'unity gain'.",
      "enum_values": [
        "1V",
        "2V",
        "4V",
        "8V",
        "12V"
      ]
    },
    {
      "name": "Output mode",
      "min": 0,
      "max": 1,
      "default": 1,
      "scope": "per-channel",
      "description": "The standard Add/Replace mode selector as described above.",
      "enum_values": [
        "Add",
        "Replace"
      ]
    },
    {
      "name": "Zero cross",
      "min": 0,
      "max": 1,
      "default": 0,
      "scope": "per-channel",
      "description": "Activates zero-cross detection. Changes in the VCA CV are only applied when the channel signal crosses 0V."
    }
  ],
  "input_ports": [
    {
      "name": "Input",
      "description": "The common input bus."
    },
    {
      "name": "Input",
      "description": "The channel input bus."
    }
  ],
  "output_ports": [
    {
      "name": "Output",
      "description": "The output bus for each channel."
    },
    {
      "name": "Output",
      "description": "The output bus. If set to 'None', the input bus is used as output, and the mode is always 'Replace'."
    }
  ]
}