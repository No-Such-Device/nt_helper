{
  "guid": "knob",
  "name": "Knob Recorders",
  "short_description": "Configures the Knob Recorders",
  "description": "This algorithm is an intrinsic part of the Performance Page (above). It is implemented as an algorithm so that clocks can be routed into it via busses, and so that the user can choose how much memory to allocate to the knob recorders from the pool available to the preset.",
  "categories": [
    "Performance",
    "Recording",
    "UI"
  ],
  "parameters": [
    {
      "name": "Page select enable",
      "min": 0,
      "max": 1,
      "default": 0,
      "scope": "global",
      "description": "Enables the 'Page select' parameter."
    },
    {
      "name": "Page select",
      "min": 1,
      "max": 10,
      "default": 1,
      "scope": "global",
      "description": "When this parameter changes, it changes the current page in the Performance Page custom UI. This will be most useful when mapped to a MIDI CC, for example, so you can remotely control the visible page."
    },
    {
      "name": "Enable",
      "min": 0,
      "max": 1,
      "default": 1,
      "scope": "per-channel",
      "description": "Enables the channel."
    },
    {
      "name": "Time",
      "min": 1,
      "max": 1000,
      "default": 100,
      "scope": "per-channel",
      "type": "scaled",
      "is_per_channel": true,
      "is_common": false,
      "is_bus": false
    },
    {
      "name": "Clock source",
      "min": 0,
      "max": 2,
      "default": 0,
      "scope": "per-channel",
      "description": "Sets the clock source for this item: one of 'None', 'Clock input', or 'MIDI'.",
      "enum_values": [
        "None",
        "Clock input",
        "MIDI"
      ]
    },
    {
      "name": "Divisor",
      "min": 4,
      "max": 19,
      "default": 9,
      "scope": "per-channel",
      "description": "The MIDI clock divisor, if the 'Clock source' is 'MIDI'."
    },
    {
      "name": "Clock multiplier",
      "min": 1,
      "max": 96,
      "default": 1,
      "scope": "per-channel",
      "description": "A multiplier applied to the clock source."
    }
  ],
  "input_ports": [
    {
      "name": "Reset input",
      "description": "The bus to use for a reset pulse, to reset the clock dividers to step 1."
    },
    {
      "name": "Clock input",
      "description": "The bus to use for the clock, if the 'Clock source' is 'Clock input'."
    }
  ],
  "output_ports": []
}