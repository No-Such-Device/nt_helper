{
  "guid": "phsh",
  "name": "Phase Shifter",
  "short_description": "Shifts the phase of signals",
  "description": "This algorithm is based on the Phase Shifter algorithm on the disting mk4. The algorithm is a phase shifter – it generates two outputs, one of which is in a fixed phase relationship to the other. Unlike the Phaser algorithm, which is simply designed to create a swooshy sound, this algorithm is designed for mathematical accuracy, and can produce some interesting psycho-acoustic effects. It can also produce swooshy sounds.",
  "categories": [
    "Effect",
    "Phase",
    "Modulation"
  ],
  "parameters": [
    {
      "name": "Shift",
      "min": -360,
      "max": 360,
      "default": 0,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "Mix",
      "min": 0,
      "max": 100,
      "default": 100,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "Level",
      "min": -40,
      "max": 0,
      "default": 0,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "Width",
      "min": 1,
      "max": 8,
      "default": 1,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "Unshifted mode",
      "min": 0,
      "max": 1,
      "default": 0,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "+ve shift mode",
      "min": 0,
      "max": 1,
      "default": 0,
      "type": "folder",
      "is_bus": false
    },
    {
      "name": "-ve shift mode",
      "min": 0,
      "max": 1,
      "default": 0,
      "type": "folder",
      "is_bus": false
    }
  ],
  "input_ports": [
    {
      "name": "Input",
      "description": "The first audio input bus."
    },
    {
      "name": "Shift input",
      "description": "The bus to use for the phase shift CV, scaled as 5V per 360°."
    }
  ],
  "output_ports": [
    {
      "name": "Unshifted output",
      "description": "The bus to use for the unshifted output signal. Note that this is not the same as the dry signal unless the input is a pure sine wave."
    },
    {
      "name": "+ve shift output",
      "description": "The bus to use for the positive shifted output."
    },
    {
      "name": "-ve shift output",
      "description": "The bus to use for the negative shifted output."
    }
  ]
}
