{
  "guid": "oscs",
  "name": "Oscilloscope",
  "short_description": "Oscilloscope for viewing waveforms",
  "description": "This algorithm implements a simple but useful 2-channel oscilloscope.",
  "categories": [
    "Display",
    "Analysis",
    "Utility"
  ],
  "gui_description": "The display shows one or both waveforms according to the display mode.",
  "parameters": [
    {
      "name": "Input 1",
      "min": 1,
      "max": 64,
      "default": 1,
      "description": "The bus for input 1."
    },
    {
      "name": "Input 2",
      "min": 1,
      "max": 64,
      "default": 2,
      "description": "The bus for input 2."
    },
    {
      "name": "Trigger type",
      "min": 0,
      "max": 4,
      "default": 0,
      "description": "The type of trigger, to synchronise the display to the waveform.",
      "enum_values": [
        "Free running",
        "1 rising",
        "1 falling",
        "2 rising",
        "2 falling"
      ]
    },
    {
      "name": "Trigger voltage",
      "min": -11.0,
      "max": 11.0,
      "default": 0.0,
      "description": "Set the trigger voltage – the voltage the input signal has to cross to trigger the oscilloscope."
    },
    {
      "name": "Time range",
      "min": 0,
      "max": 16,
      "default": 7,
      "description": "Sets the time range corresponding to the full width of the display. The special value \"Auto\" sets the time range to the time between consecutive triggers."
    },
    {
      "name": "Display mode",
      "min": 0,
      "max": 4,
      "default": 2,
      "description": "Sets which waveforms are displayed and how.",
      "enum_values": [
        "Overlaid",
        "Split",
        "Channel 1",
        "Channel 2",
        "XY"
      ]
    },
    {
      "name": "Draw mode",
      "min": 0,
      "max": 2,
      "default": 0,
      "description": "Sets how the waveform signals are drawn.",
      "enum_values": [
        "Lines (antialiased)",
        "Lines",
        "Points"
      ]
    },
    {
      "name": "Vertical scale 1",
      "min": 0,
      "max": 3,
      "default": 0,
      "description": "Sets the vertical scale for channel 1. The options are 10V, 5V, 2V, and 1V."
    },
    {
      "name": "Vertical scale 2",
      "min": 0,
      "max": 3,
      "default": 0,
      "description": "Sets the vertical scale for channel 2."
    },
    {
      "name": "Vertical offset 1",
      "min": -11.0,
      "max": 11.0,
      "default": 0.0,
      "description": "Sets an amount by which to move the display of channel 1 vertically."
    },
    {
      "name": "Vertical offset 2",
      "min": -11.0,
      "max": 11.0,
      "default": 0.0,
      "description": "Sets an amount by which to move the display of channel 2 vertically."
    },
    {
      "name": "Disable screensaver",
      "min": 0,
      "max": 1,
      "default": 0,
      "description": "If set, and the oscilloscope custom UI is displayed, the screensaver is prevented from starting. The 0V line is also disabled, to prevent screen burn-in."
    }
  ],
  "input_ports": [],
  "output_ports": []
}